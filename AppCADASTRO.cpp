#include <stdio.h>
#include <string.h>
#include "leimprimeCADASTRO.h"


int main (void){
	
	ID 	 P[1];
	REGISTRO Q[1];
	CADASTRO R[1];
	
	inputID (P);
	inputREG(Q);
	inputCAD(R);
	
	outputID (P);
	outputREG(Q);
	outputCAD(R);
	
	return 0;
}

//Matheus Malvar
